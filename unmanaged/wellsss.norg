* Concept
  User-oriented S3FS mounts management. Calls S3FS implementations to mount
  directories, orchestrates them, notifies user if anything happens - i.e. NFS
  but simple.

* Features
  - ( ) engine selection (`garage`, `garage-native`, `s3fs`).
  - ( ) orchestrator selection: (`systemd`, `native`).
  - ( ) UNIX socket for server-client communication.
  - ( ) desktop notifications.
