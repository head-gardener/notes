* References
  - {./nix.html}[nix]
  - {./ssh.html}[ssh]

* What do you need
  - identity
  - known_hosts via `StrictHostKeyChecking=accept-new` and `UserKnownHostsFile=${pkgs.writeText "known_hosts" githubHK}`.
  - kinda all.
